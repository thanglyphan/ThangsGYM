<!DOCTYPE html>
<html>
<head>
  <title>MyWebsite</title>
  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <a class="navbar-brand" <%=link_to "Home", root_url %></a>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li><a <%=link_to "Profile", profile_path %></a></li>
          <li><a <%=link_to "Settings", setting_path %></a></li>

          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
             Ulike valg<span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="group_training"> Gruppetimer</a></li>
              <li><a <%=link_to "BMI kalkulator", bmi_calc_path %></a></li>
              <li><a <%=link_to "Øvelser", exercises_body_path %></a></li>
            </ul>
          </li>
        </ul>

        <ul class="nav navbar-nav navbar-right">
          <% if @current_user.nil? %>
              <li><%=link_to "Log in", login_path %></li>
          <% else %>
              <% if @current_user.admin? %>
                  <li class="form-group">
                    <%= form_tag(:action => 'search_user', :controller => 'admins') do %>
                        <%= text_field_tag(:user_or_email) %>
                        <button type="submit" class="btn btn-default">Submit</button>
                        </li>
                    <% end %>
              <% end %>
              <li class="dropdown">
                <a class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                  <% if @current_user.profile_pic.nil? %>
                      <img src="http://placehold.it/30x30" width="30" height="30" class="img-circle special-img">
                  <% else %>
                      <img src="<%= @current_user.profile_pic %>" width="30" height="30" class="img-circle special-img">
                  <% end %>
                  <%="Hi, #{@current_user.username}!"%><span class="caret"></span></a>
                <ul class="dropdown-menu">
                  <li><a href="status">Status</a></li>
                  <li><a href="my_booking">My bookings <span class="badge">4</span></a></li>
                  <li role="separator" class="divider"></li>
                  <li><a href="logout">Log out</a></li>
                </ul>
          <% end %>
          </li>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>
  <%= include_gon %>
  <%= stylesheet_link_tag "bootstrap.css" %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
<%= javascript_include_tag 'braintree', 'data-turbolinks-track' => true %>

<%= yield %>

</body>

<footer>
  <div class="navbar navbar-fixed-bottom" id="bs-example-navbar-collapse-1">
    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <ul class="nav navbar-nav">
          <li><a <%=link_to "Admin", adminpage_path %></li>
        </ul>
      </div>
    </nav>
  </div>
</footer>
</html>



