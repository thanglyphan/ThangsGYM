

<div class="jumbotron">
  <div class="container">
    <div class="row">
      <div class="col-md-6"><span class="pull-left">
          <%= form_tag(:action => 'add_items') do %>
            <p><strong>Add a new item</strong></p>
            <p>Program:</br> <%= text_field_tag(:program) %></p>
            <p>Price:</br> <%= text_field_tag(:price) %></p>
            <p>Picture(not needed):</br> <%= text_field_tag(:picture) %></p>
            <%= submit_tag("Add new item to database") %>
            <% end %>
      </span></div>

      <div class="col-md-6"><span class="">
            <%= form_tag(:action => 'delete_items') do %>
            <p><strong>Delete an existing item</strong></p>
            <p>Choose program:</p>
            <%= select_tag(:program, options_for_select(@items))%>
            <p></p>
            <%= submit_tag("Delete this item") %>
      <% end %>
      </span></div>
      <div class="col-md-6"><span class="pull-right">
            <%= form_tag(:action => 'update_items') do %>
            <p><strong>Update an item</strong></p>
            <p>Choose program:</p>
            <%= select_tag(:program, options_for_select(@items))%>

            <p>New program:</br> <%= text_field_tag(:new_program) %></p>
            <p>New price($):</br> <%= text_field_tag(:new_price) %></p>
            <p>Picture(not needed):</br> <%= text_field_tag(:new_picture) %></p>
            <%= submit_tag("Update this item") %>
            <% end %>
      </span></div>
    </div>
    <% for user in @is_coaching do %>
        <% if !user.coach.nil?%>
            <p><strong><%= user.coach.name %></strong> is currently coaching  <strong><%= user.username %> </strong></p>
        <% end %>
    <% end %>

  </div>
</div>

